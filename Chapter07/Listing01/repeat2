#: Title       : Repeat
#: Date        : 2016-05-21
#: Author      : "Kybernetes" <correodelkybernetes@gmail.com>
#: Version     : 1.0
#: Description : Bash function declarations file
#:             : Adds repeating string capabilities
#: Options     : N/A

## Function declarations
_repeat() { #@ DESCRIPTION: Repeat a string N times
            #@ USAGE: _repeat STRING NUMBER
## Function header
    local ref_rep=${1:?"$FUNCNAME: Missing name argument"}
    local repstr=${2?"$FUNCNAME: Missing argument"}
    local -i num=${3:-1}
    shift $#

    while (( ${#repstr} < $num )); do          ## Loop until string exceeds desired length
	repstr+=$repstr$repstr                 ## 3 seems to be the optimum number
    done
    printf -v $ref_rep "%s" "${repstr:0:$num}" ## Trim to desired length & write result
}
repeat() {
    local repstr=${1?"$FUNCNAME: Missing argument"}
    local -i num=$2
    shift $#

    local __rpt=
    _repeat __rpt "$repstr" $num
    printf "%s\n" "$__rpt"
}
