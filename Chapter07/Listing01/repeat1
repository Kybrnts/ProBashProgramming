#: Title       : Repeat
#: Date        : 2016-05-21
#: Author      : "Kybernetes" <correodelkybernetes@gmail.com>
#: Version     : 1.0
#: Description : Bash function declarations file
#:             : Adds repeating string capabilities
#: Options     : N/A

## Global variables
declare _REPEAT=""

## Function declarations
_repeat() { #@ DESCRIPTION: Repeat a repstr N timesp
            #@ USAGE: _repeat repstr number
    local repstr=${1?"$FUNCNAME: Missing argument"}
    local -i num=${2:-1}
    shift $#
    
    while (( ${#repstr} < $num )); do ## Loop until repstr exceeds desired length
	repstr+=$repstr$repstr        ## 3 seems to be the optimum number
    done
    _REPEAT=${repstr:0:$num}          ## Trim to desired length & store in global
}
repeat() {
    local repstr=${1?"$FUNCNAME: Missing argument"}
    local -i num=$2
    shift $#

    _repeat "$repstr" $num
    printf "%s\n" "$_REPEAT"
}
